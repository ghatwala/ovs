language: c

compiler: 
    - gcc

os:
    - linux

cache:
  directories:
    - dpdk-dir

addons:
  apt:
    packages:
      - bc
      - libssl-dev
      - llvm-dev
      - libjemalloc1
      - libjemalloc-dev
      - libnuma-dev
      - python3-sphinx
      - libelf-dev
      - selinux-policy-dev
      - libunbound-dev
      - libunwind-dev
      - python-dev
      - python-pip
      - python-setuptools
      - python3-dev
      - python3-setuptools
      - python3-pip
      - libtool

before_install: ./.travis/${TRAVIS_OS_NAME}-prepare.sh

before_script: export PATH=$PATH:$HOME/bin

matrix:
  include:
    - os: linux
      arch: arm64
      compiler: gcc
      env:  DISABLE_SPARSE=1 DPDK_SHARED=1
script: ./.travis/${TRAVIS_OS_NAME}-build.sh $OPTS

notifications:
  email:
    recipients:
      - lance.yang@arm.com
